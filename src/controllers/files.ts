import CustomResponse, { sendJSONResponse } from "../utils/CustomResponse";
import multer from "multer";
let storage  = 
export function handlePostFiles(req: Request, res: Response) {
  res.json({ ham: "ha" });
}
